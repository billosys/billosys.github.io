# Closing report — Slice 05 Self-host marketing fonts

Closed 2026-07-01. Build gate: CC on stock Cobalt 0.20.2. CDC cross-checked on
disk and corrected the ledger per CC's review.

## Verdict

**Delivered.** The marketing landing serves its typefaces (Lora, Brygada 1918,
Recursive) from `/assets/fonts/` with **zero Google Fonts requests**; CC
confirmed `site/secondary.css` now carries the `@font-face` rules (11, was 0) and
Recursive's variable axes work self-hosted. Google Fonts is gone from every live
template. No regression to the blog (slices 01–04).

## Per-row walk (reconciled)

M-01…M-10 → `reproduced` by CC. **M-08 reconciled with a correction:** the row (and
slice-doc) originally claimed the landing used Fraunces/Source Serif and this slice
fixed a Georgia-fallback bug. CC's review — verified independently by CDC
(`_secondary-page.scss` references `--font-display`/`--font-body` zero times) —
showed the landing never uses those two families. The `@import "fonts"` was still
required (for Brygada/Lora/Recursive); it also emits Fraunces/Source-Serif
`@font-face` into `secondary.css` that are unused and never fetched (harmless).
Docs corrected; claim retracted.

## Bubble-up to the project

1. **Delivered its piece:** the marketing no-CDN gap (slice 03 bubble-up) is
   closed — marketing fonts self-hosted.
2. **Correction of record:** the Fraunces/Source-Serif fallback claim was wrong
   and is retracted (CC catch). Lesson: verify token *usage per stylesheet*, not
   just token *definition*, before asserting a fix.
3. **Carried forward (candidate cleanup, not blocking):** `secondary.css` ships
   2 unused families (Fraunces/Source Serif) — a marketing-only fonts partial
   would trim them. Also: `default.liquid`/`main.css` exists and references
   Fraunces/Source Serif, but no content page uses that layout today.
4. **Not a regression:** CC ran only `cobalt build` (not the pagefind post-step),
   so `site/pagefind/` is absent in that build — regenerated by
   `scripts/preview.sh` / `scripts/deploy.sh`. Slice-04 code untouched.

## Project status

Five slices closed: 01 blog · 02 syntax highlighting · 03 blog fonts · 04 search
· 05 marketing fonts. The **only** remaining external request anywhere on the
site is the blog author's GitHub avatar. Everything else is self-hosted.
