<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% if page.title %}{{ page.title }} — {{ site.title }}{% else %}{{ site.title }}{% endif %}</title>
  <meta name="description" content="{{ page.description | default: site.description }}">

  {% comment %} All fonts self-hosted via /secondary.css (@font-face in
     _sass/_fonts.scss) — no Google Fonts, no runtime CDN. {% endcomment %}
  {% include 'favicon.liquid' %}
  <link rel="stylesheet" href="/secondary.css">

  <meta property="og:title" content="{{ page.title | default: site.title }}">
  <meta property="og:description" content="{{ page.description | default: site.description }}">
  <meta property="og:url" content="{{ site.base_url }}{{ page.permalink | prepend: "/" | replace: "//", "/" }}">
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="{{ site.title }}">

  <link rel="canonical" href="{{ site.base_url }}{{ page.permalink | prepend: "/" | replace: "//", "/" }}">
</head>

<body>

  <nav class="nav">
    <div class="nav-inner">
      <a href="#" class="nav-brand">
        <img class="nav-logo" src="/assets/images/logo-v4.3.1-long.png" alt="Billo Systems">
      </a>
      <ul class="nav-links">
        <li><a href="#">Solutions</a></li>
        <li><a href="#">Services</a></li>
        <li><a href="#">About</a></li>
        <li><a href="/articles/">Articles</a></li>
        <li><a href="#">Contact</a></li>
        <li class="toggle-li">
          <button class="theme-toggle" id="themeToggle" aria-label="Toggle between dark and light theme">
            <span class="toggle-icon toggle-moon">☾</span>
            <span class="toggle-icon toggle-sun">☼</span>
          </button>
        </li>
        <li><a href="#" class="nav-cta">Get Started</a></li>
      </ul>
    </div>
  </nav>

  <main>
    {{ page.content }}
  </main>

  {% include 'site-footer.liquid' %}

  <script>
    // Theme Toggle
    const toggle = document.getElementById('themeToggle');
    const html = document.documentElement;

    function getPreferred() {
      const stored = localStorage.getItem('billo-theme');
      if (stored) return stored;
      return window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
    }

    function setTheme(theme) {
      html.setAttribute('data-theme', theme);
      localStorage.setItem('billo-theme', theme);
    }

    setTheme(getPreferred());

    toggle.addEventListener('click', () => {
      setTheme(html.getAttribute('data-theme') === 'dark' ? 'light' : 'dark');
    });

    window.matchMedia('(prefers-color-scheme: light)').addEventListener('change', (e) => {
      if (!localStorage.getItem('billo-theme')) setTheme(e.matches ? 'light' : 'dark');
    });

    // Scroll Reveal
    const obs = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); obs.unobserve(e.target); } });
    }, { threshold: 0.1, rootMargin: '0px 0px -60px 0px' });
    document.querySelectorAll('.reveal').forEach(el => obs.observe(el));
  </script>

</body>
</html>
